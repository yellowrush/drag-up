# 小朋友微信协作开发指南

这份规则用于把小朋友在微信里的游戏想法，变成一个独立的开发任务、一个 Pull Request、一次自动上传的微信体验版。小朋友负责提出想法和试玩反馈，Codex 负责按规则修改和验证，你负责最终决定 PR 是否合并到 `develop`。

## 一句话流程

想法箱提交 -> 自动生成 GitHub Issue -> Codex 判断任务大小 -> 创建 `codex/kid-idea-*` 分支 -> 修改代码 -> 开 PR -> CI 自动上传体验版 -> 小朋友试玩 -> 你决定是否 merge。

## 小朋友怎么提想法

小朋友优先打开“Drag Up 小朋友想法箱”网页提交想法。网页地址部署后固定发到微信里置顶。

不用写得很正式，只要尽量说清楚这三件事：

1. 我想改/加什么？
2. 我觉得这样哪里更好玩？
3. 试玩时重点看哪里？

可以提这些类型的任务：

- 改一个旧关卡，让它更简单、更难或更有趣。
- 加一个新关卡。
- 改提示文字、奖励文字、按钮文案。
- 提一个玩法想法，比如“会移动的平台”“两条路都能通关”“猫爪旋转更明显”。
- 反馈体验问题，比如“这里看不懂”“这里太难”“这里好像卡住了”。

提交后，想法箱会自动创建 GitHub Issue，并在网页里的“任务进度”区域显示链接和状态。默认不会自动开发；你审核后再给 Issue 加 `copilot-ready` 或 `codex-ready` 标签。

## Codex 开始前先判断

每个微信想法先分成三类，再决定怎么做：

- 小改动：可以直接做，例如文案、单个关卡、小范围数值调整。
- 需要拆分：想法很好，但要拆成多个 PR，例如同时改玩法、界面和很多关卡。
- 暂不适合做：风险太高或需要成人先确认，例如发布、密钥、账号、云函数、支付、隐私、删除大量代码。

Codex 回复任务时要用简单的话说明：

- 这次准备改什么。
- 会不会影响很多地方。
- 试玩时应该重点检查什么。

## 分支和 PR 规则

每个小朋友想法都单独开一个分支和一个 PR。

- 分支名：`codex/kid-idea-简短主题`
- PR 标题：`小朋友想法：简短主题`

PR 描述固定包含：

```markdown
## 小朋友原始想法

## 实际改动

## 试玩重点

## CI 体验版状态

## 需要特别审核的风险点
```

不要把多个不相关想法塞进同一个 PR。一个 PR 越小，小朋友越容易试玩，你也越容易判断能不能合并。

## 派给开发 Agent

默认 Issue 只会进入任务箱，不会自动派给 Agent。

- 加 `copilot-ready`：GitHub Actions 会把 Issue 分配给 GitHub Copilot coding agent。
- 加 `codex-ready`：GitHub Actions 会把 Issue 分配给 Codex coding agent。
- 不要同时加两个 ready 标签。选择 `copilot-ready` 或 `codex-ready` 其中一个自动 PR 流程。

Copilot 需要仓库 Secret `COPILOT_AGENT_TOKEN`。这个 token 需要只授权本仓库，并具备 Issues、Actions、Contents、Pull requests 的读写权限。

## 自动体验版流程

GitHub Actions 会在这些情况构建并上传微信体验版：

- PR 打开、更新、重新打开、标记为 ready for review。
- 推送到 `develop`。
- 你在 GitHub Actions 页面手动运行。

PR 体验版的上传描述会包含 PR 编号、分支名和短 commit，例如：

```text
PR #12 codex/kid-idea-level-5: abc1234
```

`develop` 的上传描述会标记为主线体验版。自动上传的是微信体验版，不是正式发布。

## 成人审核清单

合并 PR 前，你至少检查这些点：

- 小朋友原始想法有没有被保留。
- 这次 PR 是否只做了一个清楚的主题。
- CI 是否构建并上传成功。
- 体验版是否能打开、能试玩、没有明显卡死。
- 关卡是否真的能通关，难度是否适合。
- 文案是否适合小朋友理解。
- 是否没有改到密钥、账号、云函数、发布配置等高风险内容。

如果是 Yarn Time 关卡，还要特别注意：

- 地图整体保持从左下到右上。
- 横向宽度不超过 16。
- 不只是数据看起来合理，要实际验证连通和可通关。

## 安全边界

小朋友不能直接做这些事：

- 操作 GitHub Secrets、微信后台、云函数部署。
- 上传正式版或发布正式版。
- 接触私钥、AppID 密钥、token、账号密码。
- 合并 PR 到 `develop`。
- 要求 AI 删除大量文件或重写整个项目。

如果微信里出现密钥、账号或其他敏感信息，不要继续传播，把内容删掉并换新的密钥。

## 本地常用命令

Windows PowerShell 里优先用 `npm.cmd`。

```powershell
npm.cmd run build:minigame
npm.cmd run dev:minigame
```

上传由 GitHub Actions 负责。只有你明确授权时，才可以本地运行上传相关命令。

## 小朋友试玩反馈模板

试玩后可以简单回复：

```text
能不能通关：
哪里最好玩：
哪里太难/太简单：
有没有看不懂的地方：
还想改什么：
```

反馈不要求正式，能帮助下一次 PR 更清楚就行。
